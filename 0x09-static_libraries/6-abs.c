#include "main.h"

/**
* _abs - computers the absolute value of an integer
* @n: the int to check
* REturn: the absolute value of int
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
